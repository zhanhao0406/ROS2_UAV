# 安装完成后的初步测试（并未安装XTDrone2）

由于之前在测试PX4与QGC的完整工作流程中，QGC中显示为not ready，无法进行起飞测试。  
因此，推荐：验证核心系统，使用PX4自带的不依赖gazebo的仿真器jMAVSim：  
```bash
cd ~/PX4-Autopilot
make px4_sitl jmavsim
# 然后新建一个窗口,目的为了打开QGC
cd ~
./QGroundControl-x86_64.AppImage 
```

查看最新chatgpt的顺序
